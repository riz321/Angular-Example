import { CreateButtonDirective } from './create-button.directive';

describe('CreateButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateButtonDirective();
    expect(directive).toBeTruthy();
  });
});
